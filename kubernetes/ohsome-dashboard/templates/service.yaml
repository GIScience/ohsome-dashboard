apiVersion: v1
kind: Service
metadata:
  name: {{ include "ohsome-dashboard.fullname" . }}
  labels:
    {{- include "ohsome-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ohsome-dashboard.selectorLabels" . | nindent 4 }}
